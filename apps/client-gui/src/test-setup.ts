/*
 * @no-test  The test harness itself: it is loaded before every test file, so a
 * test asserting that it works could only pass by having already worked.
 */
import "@testing-library/jest-dom/vitest";
